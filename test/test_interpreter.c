#include "interpreter.h"
#include "nodes.h"
#include "C.tab.h"
#include "token.h"

NODE* test_case_plus(){
    NODE *x1 = make_leaf(new_token(INT));
    NODE *x2 = make_leaf(new_token())

    NODE* tree = make_node('~',)
}